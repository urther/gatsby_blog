import styled from '@emotion/styled'

export const FooterWrapper = styled('footer')`
  background-color: #f5f5f7;
  color: #1d1d20;
  display: grid;
  place-items: center;
  margin-top: auto;
  padding: 50px 0;
  font-size: 14px;
  text-align: center;
  line-height: 1.5;
`
